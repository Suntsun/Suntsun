# Falsos positivos verificados del escaner de secretos (guardian_sombras).
# Una entrada por linea; se compara como subcadena del texto detectado,
# y ese texto llega TRUNCADO: hay que whitelistear el fragmento corto.

# Seccion "Contacto": insignia de correo de contacto. El escaner la clasifica
# como "credenciales en URL" por el esquema mailto:, pero es una direccion
# publica de contacto, no una credencial.
mailto:mrm.sunsun@
